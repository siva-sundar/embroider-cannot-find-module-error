import index from 'embroider-cannot-find-module-error/utils/index';
import { module, test } from 'qunit';

module('Unit | Utility | index', function() {

  // Replace this with your real tests.
  test('it works', function(assert) {
    let result = index();
    assert.ok(result);
  });
});
